#include <stdio.h>
/**
 *  main - Prog Entry point
 *
 *  Return: Always 0 (success)
*/
/* betty style doc for function main goes there */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);
	putchar('\n');
	return (0);
}
